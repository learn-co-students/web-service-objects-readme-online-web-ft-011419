<ul>
  <% if @friends.length > 0 %>
    <% @friends.each do |friend| %>
      <li><%= "#{friend['firstName']} #{friend['lastName']}" %></li>
    <% end %>
  <% else %>
    <p>Seems that you do not have any friends. Sad.</p>
  <% end %>
</ul>
